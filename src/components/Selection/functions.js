export const ageText = (value) => {
    return `${value}`
  }
  
 export const donorAgeText = (value) => {
      return `${value}`
    }
  
  export const praText = (value) => {
    return `${value}`
  }
  
  export const kdriText = (value) => {
      return `${value}`
    }

    export const kdpiText = (value) => {
        return `${value}`
      }
  